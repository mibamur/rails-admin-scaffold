<% for attribute in attributes -%>
%dl.dl-horizontal
  %dt
    %strong <%= attribute.human_name %>:
  %dd= @<%= singular_table_name %>.<%= attribute.name %>
<% end -%>

= link_to 'Редактировать', edit_<%= prefixed_plain_model_url %>_path(@<%= singular_table_name %>), class: 'btn btn-default'
= link_to 'Назад', <%= prefixed_index_helper %>_path, class: 'btn btn-default'
